<UserControl x:Class="dot_shop.userControls.AdView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:dot_shop.userControls"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="800">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="22"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Label Name="lblName" HorizontalAlignment="Stretch" Content="REKLAMĄ" VerticalAlignment="Stretch" HorizontalContentAlignment="Left" VerticalContentAlignment="Bottom" />
        <materialDesign:Card Margin="3,0,3,0" Grid.Row="1">
            <Grid>
                <ProgressBar x:Name="progressBar" Style="{StaticResource MaterialDesignCircularProgressBar}" Margin="2,2,2,2" Value="0" IsIndeterminate="True" />
                <Image Name="imgAdPrev"/>
            </Grid>
        </materialDesign:Card>

    </Grid>
</UserControl>
